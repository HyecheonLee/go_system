package main

func main() {
	/*	pk, err := procon_genkeys.PrivateKeyToEncryptedPEM(1028, "system")
		if err != nil {
			fmt.Println(err)
		}
		f, ok, err := procon_fs.CreateFile(procon_config.KeyCertPath, "mykey.pem")
		if !ok {
			fmt.Println(err)
		} else {
			procon_fs.WriteFile(f, pk)
		}*/
}
